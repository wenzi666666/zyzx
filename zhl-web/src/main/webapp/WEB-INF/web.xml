<!DOCTYPE web-app PUBLIC
 "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
 "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app>
	<display-name>zhl cloud platform</display-name>
	<!-- 加载顺序 content-param -> listener -> filter -> servlet -->

	<!-- Spring -->
	<!-- 配置Spring配置文件路径 -->
	<!-- 在web环境中一定要定位到classpath 否则默认到当前web应用下找 如果文件在jar包内或者其它项目里，就需要使用classpath* -->
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>
			classpath*:applicationContext-*.xml
			classpath*:applicationContext.xml
		</param-value>
	</context-param>
	<!-- 配置Spring上下文监听器 -->
	<listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
	</listener>
	<listener>
		<listener-class>org.springframework.web.util.Log4jConfigListener</listener-class>
	</listener>
	<!-- 防止spring内存溢出监听器 -->
	<listener>
		<listener-class>org.springframework.web.util.IntrospectorCleanupListener</listener-class>
	</listener>	
	<!-- Spring -->

	<!-- 配置log4j配置文件路径 -->
	<context-param>
		<param-name>log4jConfigLocation</param-name>
		<param-value>classpath:log4j.properties</param-value>
	</context-param>

	<!-- Shiro配置
	<filter>
		<filter-name>shiroFilter</filter-name>
		<filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
		<init-param>  
            <param-name>targetFilterLifecycle</param-name>  
            <param-value>true</param-value>  
        </init-param>
	</filter>
	<filter-mapping>
		<filter-name>shiroFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	 -->

	<!-- 配置Spring字符编码过滤器 -->
	<filter>
		<filter-name>encodingFilter</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>UTF-8</param-value>
		</init-param>
		<init-param>
			<param-name>forceEncoding</param-name>
			<param-value>true</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>encodingFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>

	<!-- Spring MVC 核心控制器 DispatcherServlet 配置 -->
	<servlet>
		<servlet-name>dispatcher</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>classpath:spring-mvc.xml</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>dispatcher</servlet-name>
		<!-- 拦截所有/api/* 的请求,交给DispatcherServlet处理,性能最好 -->
		<url-pattern>/</url-pattern>
	</servlet-mapping>
	
	<!-- 首页 -->
	<welcome-file-list>
		<welcome-file>/security/tologin</welcome-file>
	</welcome-file-list>

	<!-- 错误页 -->
	<error-page>
		<error-code>401</error-code>
		<location>/WEB-INF/views/401.jsp</location>
	</error-page>
	<error-page>
		<error-code>404</error-code>
		<location>/WEB-INF/views/404.jsp</location>
	</error-page>
	<error-page>
		<error-code>500</error-code>
		<location>/WEB-INF/views/500.jsp</location>
	</error-page>





	
	
	
	
    
<!-- 资源应用访问路径   为此web服务的访问方式   一般为 http://ip:端口/    必须以‘/’结尾  -->
	<context-param>
		<param-name>host</param-name>
			<param-value>http://192.168.111.101:8080/zhl-resources/</param-value>
	</context-param>
<!-- 资源应用访问LOCAL   为此web服务的访问方式   一般为 http://ip:端口/    必须以‘/’结尾  -->
	<context-param>
		<param-name>hostLocal</param-name>
		<param-value>http://192.168.111.101:8080/zhl-resources/</param-value>
	</context-param>
  <!-- 文件服务器 （IIS服务器地址 传、下载、访问）    一般为 http://ip:端口/down/    必须以‘/’结尾 其中down为文件服务     --> 
  <context-param>
    <param-name>resService</param-name>
    <param-value>http://192.168.111.22:8099/down/</param-value>
  </context-param>
  <!-- 文件服务器 （IIS服务器地址 传、下载、访问）  一般为 http://ip:端口/down/    必须以‘/’结尾  --> 
  <context-param>
    <param-name>resServiceLocal</param-name>
    <param-value>http://192.168.111.22:8099/down/</param-value>
  </context-param> 
	
	

</web-app>
